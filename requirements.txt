# Core dependencies for the nutrition5k_pkg training / inference package.
#
# Target runtime: Google Colab (Python 3.12, CUDA 12.x). Versions are pinned to the
# current Colab runtime (May 2026) so reproduction matches the training environment.
# Install with:  pip install -r requirements.txt
#
# The Gradio web demo pins its own dependencies in webapp/requirements.txt.

# --- Deep learning (Colab runtime, May 2026) ---
torch==2.10.0
torchvision==0.25.0

# --- Data, config & utilities ---
numpy==2.0.2
pandas>=2.2,<2.3
PyYAML==6.0.3
Pillow==12.0.0
tqdm>=4.66

# --- Optional: backbone ablations (ConvNeXt / Swin-V2, see configs/colab/ablations/) ---
# Not part of the Colab base image; pip-installed on demand. backbone.py lazy-imports it.
timm>=1.0,<2.0
